import { ImageUrlValidDirective } from './image-url-valid.directive';

describe('ImageUrlValidDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageUrlValidDirective();
    expect(directive).toBeTruthy();
  });
});
